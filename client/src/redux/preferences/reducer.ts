const initialState = {};

const preferencesReducer = (state = initialState, action: any) => {
  // things
  switch (action.type) {
    default:
      return state;
  }
};

export default preferencesReducer;
